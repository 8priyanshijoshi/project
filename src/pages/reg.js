// reg.js

// Regular expression to validate full names (only letters and spaces)
export const fnamereg = /^[A-Za-z\s]+$/;

// Regular expression to validate email format
export const emailreg = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
